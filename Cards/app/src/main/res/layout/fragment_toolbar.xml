<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
       xmlns:card_view="http://schemas.android.com/apk/res-auto"
       android:layout_width="match_parent"
       android:layout_height="wrap_content">

    <android.support.v7.widget.CardView
        android:onClick="onToolbarClicked"
        android:layout_width="match_parent"
        android:layout_height="@dimen/height_toolbar"
        card_view:cardElevation="2dp"
        card_view:cardBackgroundColor="@color/white_shadow">

        <com.maxcriser.cards.view.TextViews.RobotoRegularTextView
            android:textStyle="bold"
            android:layout_gravity="bottom"
            android:textColor="@color/text_toolbar"
            android:id="@+id/title_toolbar"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/height_toolbar"
            android:layout_marginLeft="@dimen/margin_left_title_toolbar"
            android:gravity="center"
            android:text="@string/app_name"
            android:textSize="18sp"/>

        <ImageButton
            android:layout_gravity="bottom"
            android:layout_width="@dimen/height_toolbar"
            android:layout_height="@dimen/height_toolbar"
            android:background="?attr/selectableItemBackground"
            android:onClick="onMenuClicked"
            android:src="@drawable/ic_menu_black_24dp" android:id="@+id/imageButton"/>

    </android.support.v7.widget.CardView>

</merge>